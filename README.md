# BetterHome3D

**让 AI 帮你搭建一个可以浏览、移动家具、比较选款并原位批注的 3D 家。**

BetterHome3D 面向想用 AI 设计、比较和反复调整室内空间的人，以及为他们工作的 Codex 等编码 Agent。你提供需求和参考图，Agent 负责选择现成模型、安排布局和配色；你在浏览器里看空间、拖家具、换款，直接在想修改的位置留下评论，再让 Agent 按评论继续调整。

项目包含一份可安装的 **skill** 和一套可运行的 **Three.js / WebGPU 脚手架**：skill 说明如何准备资产、配置场景、处理反馈和验证效果；脚手架提供实际的渲染、编辑与保存评论的代码。新场景主要修改资产和 `scene.js`，复用已有的通用交互。

## 为什么不每次都让 AI 从零手写？

让 AI 从零写一个小型 3D 演示完全可行。需要反复选家具、修改设计、保留反馈并交给另一个 Agent 继续时，工作还包括场景之外的编辑、状态和验证。BetterHome3D 把这些可重复的工作保留为代码和操作说明，让 Agent 将精力更多地放在具体设计上。

这里的“手写”有两件事：用基础几何体拼家具，以及从零编写查看器。前者可以用现成模型替代，后者可以复用脚手架。

| 你要做的事 | 从零开始需要处理 | BetterHome3D 的做法与收益 |
| --- | --- | --- |
| 复现参考图中的家具 | 制作家具形体、材质和细节，或自行接入资产加载 | 优先导入现成 GLB 和 PBR 材质，把工作放在选型、比例与配色上。外观质量仍取决于模型，不自动生成家具。 |
| 自己试着挪动和旋转 | 编写拾取、变换手柄，协调镜头与家具操作 | 已有直接拖动、旋转环、WASD 镜头和按标签隐墙，可以在同一场景中试摆。 |
| 比较几款家具 | 编写替换、缓存、姿态保留和选择界面 | 配置少量候选后，点击缩略图换款，保持家具的位置与朝向，便于直接比较。 |
| 告诉 Agent 具体改哪里 | 通过文字或截图解释目标，并维护修改记录 | 评论保存对象身份、局部落点、原始截图与历史；Agent 能核对目标，修改后关闭已完成的项。 |
| 多轮修改并继续查看 | 处理重新加载、状态恢复和更新失败 | 场景在线更新保留当前镜头与仍适用的家具调整；新场景准备失败时保留旧画面。 |
| 控制运行成本并交接 | 处理绘制调度、资源释放、性能检查，再解释项目用法 | 已有按需渲染、交互期间降低分辨率、检查脚本和 skill 指引，减少重复实现与排错；帧率仍需在目标设备上测量。 |

## 一个典型使用过程

1. **提出设计需求。** 例如：“按这张参考图布置横厅，工作区替代餐区，单椅给三个候选。”
2. **Agent 准备场景。** 选择并整理现成资产，配置布局、材质、灯光、候选和观察机位，运行脚手架。
3. **在空间里反馈。** 浏览各个角度、拖动家具、比较候选；双击墙面或家具写下“这里太空”或“这把椅子换一个”。
4. **让 Agent 继续修改。** 告诉它读取评论；它核对关联对象和截图，修改场景并在线发布，检查结果后关闭已完成评论。

这套流程适合需要多轮调整的室内设计、家具选型和空间展示原型。它不包含家具库或示例房间模型，不会根据照片自动还原户型，也不替代设计者对尺度、动线和美感的判断。模型检索、尺寸整理和视觉验收仍是实际工作的一部分。

[安装为 skill](#安装为-codex-skill)，或[直接生成第一个场景](#不安装-skill直接生成场景)。完整的光照、隐墙、导出与交互配置见[脚手架说明](references/scaffold.md)。

## 安装为 Codex skill

需要 Git；生成场景还需要 Python 3.10+、Node.js 20+ / npm，以及支持 WebGPU 的浏览器与 GPU。Three.js 锁定为 `0.185.1`，由 `npm ci` 安装。

```sh
git clone https://github.com/Shire31/betterhome3D.git \
  "${CODEX_HOME:-$HOME/.codex}/skills/threejs-rendering"
```

如果目标目录已经有同名 skill，先保留现有版本，不要覆盖未同步的改动。让 Codex 重新加载 skill 后可使用：

> 使用 $threejs-rendering，根据我的参考图创建新场景。复用现成模型和脚手架，配置家具候选、空间评论及在线更新。

其他 Agent 可以直接从 [SKILL.md](SKILL.md) 开始读取。必须保留整个仓库目录；仅复制 `SKILL.md` 无法运行生成器。文中提到的 `find-3d-models` 是可选的独立检索 skill，不是启动依赖。

## 不安装 skill，直接生成场景

```sh
git clone https://github.com/Shire31/betterhome3D.git
cd betterhome3D
python3 scripts/create_scene.py ../my-scene \
  --model /absolute/path/to/chair.glb --editable --name '我的场景'
cd ../my-scene
npm ci
npm run check
npm start
```

打开 <http://127.0.0.1:8768/>。输入模型须为包含贴图和缓冲的 GLB；完整房间模型省略 `--editable`。输出目录必须不存在，生成器不会覆盖已有项目。重复的 `--model` 创建独立方案，多件家具组合需编辑 `scene.js` 的 `assets`。

服务仅监听本机，不是面向公网的多人协作服务。已占用默认端口时，可在生成项目中运行 `python3 server.py --port 8770`。

## 配置与维护

| 入口 | 内容 |
| --- | --- |
| [SKILL.md](SKILL.md) | Agent 工作流程、资产和画质检查、性能诊断、浏览器负载控制 |
| [references/scaffold.md](references/scaffold.md) | 场景配置、家具候选、隐墙、灯光、在线更新与运行时检查 |
| [references/annotations.md](references/annotations.md) | 空间关联、评论读取、处理、关闭和冲突保护 |
| [scripts/create_scene.py](scripts/create_scene.py) | 从模板和用户提供的 GLB 创建独立项目 |
| [assets/scaffold/](assets/scaffold/) | 生成项目所使用的完整运行时代码及检查 |

修改生成项目中的场景和资产后运行：

```sh
npm run check
npm run publish:scene
```

在原页面确认新版本已应用；通用运行时代码或 HTML 升级仍需要重新加载页面。验证存储和发布服务可运行 `npm run check:server`；需要浏览器的验证及临时测试项目用法见脚手架说明。

## 当前边界

- 评论持久化到项目文件；家具移动、候选选择与镜头仅保留在页面会话，刷新前须按文档备份。
- 在线更新会准备新场景并提交，复杂场景可能短暂停顿，不是任意代码热替换。
- 家具比例、原点、朝向、候选缩略图、墙体标签及灯位需显式准备；没有自动布置或碰撞检测。
- WebGPU 不可用时明确报错，不静默退回 WebGL；性能须在目标设备、分辨率与资产规模下实测。

## 许可

仓库原创代码与文档采用 [MIT License](LICENSE)。Three.js 通过 npm 安装并遵循其自身许可；用户引入的模型、贴图和图片须遵守各自的许可，本仓库不包含这些第三方资产。
