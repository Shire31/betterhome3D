---
name: threejs-rendering
description: 为室内与产品场景检索和复用现成 3D 模型，通过可运行脚手架创建 Three.js/WebGPU 查看器，支持家具平移旋转、WASD 镜头、候选比较、空间评论、光线切换与在线更新，并诊断材质、低 FPS、拖动卡顿和加载停顿。包含模型检索经验及脚本、多角度预览和 PNG 出图。
---

# Three.js 渲染与性能

让现有场景在目标设备上可用、流畅，并保留用户需要的画质。先理解实际渲染链，测量瓶颈，再改动拥有该行为的代码。复用当前 renderer、加载器和资产；用户明确要求 WebGPU 或保持画质时，按该约束完成，不能自行改成另一目标。

渲染统一使用 Three.js/WebGPU：模型库缩略图、多角度预览、场景验收及最终 PNG 都走本脚手架，不调用 Blender Cycles/EEVEE 出图。需要处理原生资产时，Blender 仅用于格式转换、网格及材质整理；完成后在 Three.js 中检查实际导出的 GLB。库内预览须对应可下载的模型版本，复用一个预览页顺序切换模型和机位，不为每张缩略图新开 renderer 或标签页。

## 快速创建与复用

需要寻找参考图相似款或新家具时，先读 [现成模型检索](references/model-sourcing.md)。其中包含模型库选择、英文 query 改写、预览筛选与失败纠偏，以及本 skill 自带的 `scripts/blendkit.py`；无需依赖另一个 skill 或本机项目目录。已有合适资产时直接复用，不重复搜索。

用户要新场景或可复用查看器时，使用本 skill 自带的 [WebGPU 脚手架](references/scaffold.md)。执行 `scripts/create_scene.py 新目录 --model 现成模型.glb`，安装锁定依赖后即可运行；布局、资产位置、灯光与机位放在生成项目的 `scene.js`，通用渲染入口无需反复重写。复用已有项目时直接改其场景配置。脚手架不含家具资产库，不用手搓家具代替用户指定的真实模型。

脚手架内置清晨、中午、傍晚、黑夜选择，复用 `lighting.js`；项目只配置初始 `timeOfDay`、窗户对应的 `sunRotation` 和实际灯位。可配置室内 PMREM 反射与静态遮光壳，切换保留镜头/家具并在失败时恢复。具体角色标签和验证见 [时段光照](references/scaffold.md#清晨中午傍晚黑夜)。

脚手架用真实 WebGPU + TSL 后处理，保留铺满窗口、按需渲染、交互降像素/静态恢复、PNG、自动隐墙标签及性能检查。家具编辑复用内置 Raycaster / TransformControls：在资产上配置 `name`、`editable`，摆件用 `attachTo` 跟随家具；单件家具可在创建命令加 `--editable`。已包含平移、旋转、复位、方案内会话状态和导出隐藏手柄。WASD／方向键沿镜头朝向水平移动相机，按住连续、松开停止，输入与失焦保护内置；复用现有按需渲染，不新增渲染循环。具体配置、启动方式、限制和验证命令见上面的参考；只在创建或维护脚手架时读取。

空间批注复用内置模块和本地保存服务：双击表面创建，稳定对象 ID、局部坐标、原视角截图和处理历史保存在项目中。标记使用 GPU 球体并随场景同步渲染；点击球体在旁边用同一输入条编辑正文，点发送确认保存，顶部按钮才打开带关闭/历史操作的全局列表。工具栏、家具编辑和评论共用 `ui.css` 的克制黑白样式。用户让你读评论并修改时，先读 [批注消费与操作](references/annotations.md)，再根据截图和当前对象核对目标；完成后可按已有授权关闭，并写具体处理说明。

处理评论后需让用户实时看到结果时，保持原服务运行，修改场景来源后执行 `npm run publish:scene`。确认页面已应用返回的版本并目视检查，再关闭对应评论。脚手架在同一页面内保留机位与无关家具调整，草稿/拖动期间延后提交；失败保留旧场景。刷新或重开不保留家具会话，详见 [家具编辑](references/scaffold.md#家具编辑)。资源管理、发布范围及编译暂停的边界见 [脚手架在线更新](references/scaffold.md#保持页面在线更新)。

## 浏览器与工作机负载

- **默认复用一个 3D 标签页。** 先定位已有项目页面，用方案按钮、机位切换和在线更新验收，不为每个方案、角度或修改版本新开页面。模型转换、结构检查、评论 JSON 和截图读取优先使用命令行、本地 API 或文件；浏览器用于实际交互、视觉和渲染测量。
- 只有需要隔离会修改数据的回归检查或通用查看器改动时，才临时增加一个测试页；使用最小代表模型，避免复制整套高负载场景。复用该测试页，结束立即关闭并停止自己启动的临时服务。保留用户正式页及其会话，不把关闭或 `dispose()` 正式页当作清理手段。
- 后台暂停绘制不等于释放模型、贴图和渲染器。测帧率前关闭自己已不用的临时预览页，并等模型转换、解码和编译结束；不要让 Blender 转换与浏览器性能测试并行。仍有其他高负载任务时记录干扰，不能把单页 FPS 当成整机流畅度。

## 接手场景

- 查看锁定的 Three.js 版本，以及模型加载 → 材质适配 → 光照/阴影 → 后处理 → 显示/导出的实际调用链。找全 resize、相机事件、场景切换和资源释放入口；不要只改动画循环而漏掉导出或加载首帧。
- 从现有设备、窗口和用户反馈确定目标。60 FPS 对应约 16.7ms 一帧，30 FPS 对应约 33.3ms；它们是预算示例，不是所有项目必须达到的指标。同时看帧时间尾部和用户操作延迟。
- 创建新查看器时先让代表性模型以基础 PBR、正确颜色和简单光照运行；后处理逐项加入并检查成本。小房间不代表模型轻量，下载的家具可能为离线渲染制作。
- 新资产进入整场前，检查求值后的三角面、材质数、贴图尺寸、包围盒和单位；先确定网页副本的成本，再做精细摆放。绒毛/曲线尤其要控制密度、每根采样点数和截面边数，保留轮廓，细节可由贴图承载。性能短测放在代表性资产接入时，不拖到全部软装完成。
- 需要 Blender 转换时，先只读探测当前版本、实际节点输入接口和求值结果，再用单个对象验证导出尺寸、轴向、非空网格和面数。旧脚本发生接口错误时，缩小到该接口核对后重试，不反复跑完整转换。已验证的转换脚本和原始资产留在项目中复用，不把某次 API 写法或面数阈值固定为通用规范。

## 建立可信基线

复用项目已有统计；缺少时只加一个能跑真实相机运动的短测或 profiler probe。不要为了测 FPS 创建另一套渲染器。

1. **记录真实条件。** 场景/资产版本、设备与浏览器、Three.js 版本、实际 renderer backend、CSS 尺寸、drawing buffer 尺寸、DPR、AO/阴影/MSAA 设置。模型原始三角面数与所有 pass 累计提交的面数分开记录。
2. **测真实交互。** 用固定、按经过时间计算的相机轨迹，覆盖旋转、缩放，以及确有问题的隐墙切换。不要让 auto-rotate 的每帧增量使高低 FPS 跑出不同路径。在现有页面空闲时复用 renderer 短测，保存并恢复机位及交互状态，保持目标窗口尺寸。会改动评论等数据的回归检查才使用上面的隔离测试页；不要为 A/B 同时打开两份完整场景。
3. **识别无效样本。** 用户拖动或切换布局、后台节流、窗口缩放、未完成加载都会污染结果。临时禁用测试页输入时，保存状态并在 `finally` 中恢复；发生干扰就丢弃样本，不把不同场景拼成 A/B。页面可见状态、场景及尺寸应在测量期间保持稳定。
4. **区分指标。** 记录平均 FPS、帧间隔 median/p95，必要时 p99/长帧数；将下载、解码、shader 编译、首帧分别计时。rAF 间隔包括浏览器调度，不能称为 GPU 时间；CPU 提交结束也不等于 GPU 完成。GPU timer 仅在可用且需要区分瓶颈时使用，排除无效/disjoint 样本。
5. **控制成本与结论。** 预热实际被测的运动和效果组合，再做数秒短测定位；首次交互、持续运动、停止后恢复画质的长帧分开检查。结论接近门槛或噪声明显时再交替复测。每次只改变一个变量，组合策略另测。更低分辨率或关闭 AO 的提速属于画质取舍，不能写成“同画质换引擎提速”。报告中的 FPS 同时注明实际渲染尺寸及运动时 AO 状态；不把该条件下的 60 FPS 扩大成多标签或整机不卡。

没有可运行的目标浏览器/GPU 时，明确哪些只是静态风险判断，并留下可执行的测量办法；不能编造 FPS 或把代码检查当成性能验证。

WebGL 多 pass 统计要在整个应用帧开始前 reset、全部 pass 完成后读取；使用 `renderer.info.autoReset = false` 避免只统计最后一遍。WebGPU 的字段与计数语义按当前版本核对。`renderer.info.memory` 的对象数量不是显存字节数。不要把 `gl.finish()` 或其他 GPU 同步等待放进日常逐帧循环；诊断性等待会扰动测量，需单独标注并移除。

## 按证据减少开销

先尝试能解释测量差异的最小改动，不要机械执行所有优化。

| 现象 / 要验证的预测 | 优先尝试 | 注意事项 |
| --- | --- | --- |
| 静止时仍持续消耗 GPU | 场景不变时不提交绘制；相机、加载完成、尺寸、灯光和按钮变化时请求更新 | 阻尼要更新到停止；动画、视频纹理和时间 shader 仍需按实际需要更新。空闲停止绘制不是交互 FPS 提升的证据 |
| 降低 drawing buffer 后明显变快 | 限制 DPR 或像素总量；必要时使用动态交互分辨率 | 像素量随 DPR 平方增长。核对 renderer 与 composer 的实际缓冲，防止重复乘 DPR；不要只记录 CSS 尺寸 |
| 关闭 AO/后处理后明显变快 | 检查全场景法线/深度 pass、AO 分辨率、采样和去噪；逐项比较半分辨率或少量采样 | GTAOPass 构造时的 512px 可能被 composer 的 resize 覆盖。主画布抗锯齿不保证中间 render target 已抗锯齿；核实 MSAA 实际发生的位置与成本 |
| 一块玻璃就增加整场景绘制 | 单独比较 transmission 开关或降低其渲染分辨率 | 不需要明显折射的薄窗玻璃可用透明近似，但要目视确认。不能把酒杯、厚玻璃等需要折射的对象一律改成低透明度；普通透明仍有排序和 overdraw 成本 |
| 阴影更新造成峰值 | 缩紧 shadow camera，控制分辨率和投影灯数量，筛选真正需要投影的物体 | 静态场景可缓存阴影；灯、遮挡物、可见性改变时需失效。普通相机移动不一定需要刷新，但相机跟随的阴影范围需要。点光源阴影通常涉及多个方向 |
| 面数集中在少数家具/织物 | 按网格及资产组排序，检查重复细分；离线生成合适的网页 LOD，微小纹理细节用贴图承载 | 保留原始资产，在网页副本上减面；检查轮廓、UV、法线、镂空和布褶。不要直接导入高模再把 Draco 当成减面 |
| draw calls/CPU 提交很多 | 复用 geometry/material；对重复物件考虑 InstancedMesh，合并静态且同材质的对象 | 总面数和 draw calls 是不同问题。合批须保留拾取、编辑和隐墙分组能力，避免超大包围盒破坏剔除 |
| 首次加载或切换卡住 | 分开测网络、Draco/纹理解码、shader 编译、纹理上传；按需使用已有异步编译与 worker 能力 | Draco 主要压缩传输，并不减少解码后的三角面。纹理 GPU 压缩可改善驻留成本，但也有转码与兼容代价；不能拿 GLB 文件大小当显存占用 |
| 多次切换后越来越慢 | 释放已不再使用的 geometry、material、texture、render target 和监听器；处理迟到加载结果 | `scene.remove()` 不会自动释放 GPU 资源。共享资源按真实所有权释放，不能销毁其他场景仍使用的贴图；固定场景反复切换后资源数量应趋于稳定 |

有 CPU 主线程证据时，再检查每帧遍历、临时对象分配、DOM 更新和重复材质编译；不要仅因为用了 JavaScript 就判定 CPU 是瓶颈。先定位最贵的对象/pass，而不是随意降低整个场景的细节。

## 分开交互、静态与导出

对静态模型/室内查看器，可以采用以下模式；用户要求运动时也保持最高画质则另行实测满足它的方案。

| 状态 | 行为 |
| --- | --- |
| 相机运动 | 使用经过验证的预览分辨率、AO/采样配置；布局、材质配色和对象可见性保持正确 |
| 运动结束 | 阻尼结束后恢复用户选择的分辨率和效果，补一帧详细画面；随后无变化则不重复绘制 |
| 导出图片 | 采用导出分辨率和用户选择的效果，导出期间协调或暂停同一 canvas 的动画绘制，避免覆盖导出缓冲 |

分辨率上限、恢复延迟和目标帧率都由目标设备与体验决定；不要把一次案例里的数值变成固定规范。用一个渲染入口维护失效与质量状态，避免“设置里勾选了 AO”和“移动时暂时未执行 AO”混为同一事实。

按需绘制的更新源不能漏掉：OrbitControls change/阻尼、resize/DPR 变化、模型或贴图完成、灯光/材质/隐墙状态改变、导出后的缓冲恢复。不要在每帧重建 renderer、composer 或 render target。程序化改变相机也必须触发更新。

家具候选通过资产的 `candidateLabel`、`thumbnail` 与 `candidates` 接入左侧竖排栏；点击局部替换并保留根节点位置和朝向。为重点家具准备少量经过风格与尺寸筛选的现成款式、正确 PBR 和真实缩略图。只预加载当前家具的一组，不为缩略图再开 WebGPU 画布。具体配置、批注换款关联与缓存边界见 [家具候选](references/scaffold.md#家具候选)。

家具编辑使用单位缩放的外层根节点，模型原有缩放放在内部；桌面摆件附到同一根节点。根节点以 YXZ 顺序表示水平朝向，锁定高度和 X/Z 倾斜，避免原生四元数更新后超过 90° 的 Y 角度反折。验证必须包含实际拖动及四元数更新，单测数值输入不能覆盖这个问题。拖动时暂停 OrbitControls，移动遮挡物时刷新阴影；键盘输入、导出和场景切换也要恢复正确交互状态。

导出用 `try/finally` 恢复尺寸、DPR、效果开关、调度及待处理 resize；检查实际 PNG Blob 的格式/尺寸并目视核对。Blob 生成成功和文件已下载到磁盘是两个结果，不能用一个旧文件的存在代替新导出验证。测试结束后还要确认预览能继续旋转、缩放和导出。

## 何时迁移 WebGPU

- 先检查安全上下文、`navigator.gpu`、adapter 获取结果，再确认渲染器实际 backend。存在 API 或使用 `WebGPURenderer` 类名，都不证明运行在 WebGPU；可能发生 WebGL2 回退。
- 用户明确要求 WebGPU 时做迁移并验证；只是询问提速方案时，根据测量选择收益明确的路线。WebGPU 不会自动消除高面数、材质数量、像素填充或解码开销，也不保证比当前 WebGL 更快。
- 按项目锁定版本核对官方迁移文档和本地源码。以 Three.js 0.185.x 的管线为例，`EffectComposer/GTAOPass/OutputPass` 不能直接搬到 WebGPU，需迁移到对应的 node/TSL 后处理；自定义 GLSL 材质也需逐项评估。
- 同时追踪异步初始化与 PMREM、矩形灯支持、阴影/透明材质、能力查询、性能统计及导出。不要把 WebGL 的 `getContext().finish()` 等调用照搬过去。GLTF 的常见 PBR 材质及场景布局通常可复用，但仍需视觉回归。
- 比较引擎时固定相机、几何、实际像素数、效果和缓存条件，并记录实际 backend。若算法或画质不同，明确它们不同，不据平均 FPS 单独宣布某个引擎更优。

## 视觉与交付检查

- 正确处理颜色贴图与法线/粗糙度等数据贴图的色彩空间，以及 tone mapping/output 转换。优化不能把木纹覆盖成纯色、让模型发白，或重复应用输出转换。程序材质导出不支持的部分需有明确回退，不能默默变成默认灰色。
- 查看同机位的运动预览、静态画面和导出图，特别检查透明窗帘、玻璃、阴影、细线、布料、重复物件。不要全局关闭法线或阴影来掩盖某个资产的问题。
- 隐墙应按稳定对象分组切换，门框/挂画等依附构件随墙处理；同时验证阴影缓存是否正确失效。不要为转动视角每帧重建房间。
- 留下项目内最小可运行检查：实际相机运动及帧时间、静止期间实际绘制次数、质量恢复、场景切换/迟到加载、导出后恢复。只覆盖本次改动相关行为，不另造通用测试框架。
- 报告注明测试设备与窗口、改动前后指标、画质取舍、验证范围和仍存在的长帧。平均 47 FPS 而 p95 仍为 50ms 时，只能说平均交互改善，不能声称稳定 60 FPS 或完全不卡。

## 已验证案例与适用边界

一次本地室内查看器在 Three.js 0.185.1、1265×1265 显示缓冲下，布局 1 从约 15.4 FPS 改善到 47.3 FPS：薄窗玻璃去掉折射额外 pass，运动时关闭 GTAO 并使用 900×900 预览，停下约 180ms 后恢复显示分辨率和 AO。静止 600ms 内新增绘制为 0。其 p95 仍约 49.9ms，桌面其他负载未隔离；这证明该组合值得测试，不是通用性能承诺，也不是同画质的 WebGL/WebGPU 对照。无需依赖原项目路径或复制它的房间坐标才能使用本 skill。

## 按需核对的官方资料

API 与后处理支持会变化；实施前以项目实际版本为准。

- [WebGLRenderer：统计、编译、transmission 等接口](https://threejs.org/docs/pages/WebGLRenderer.html)
- [按需渲染与 OrbitControls 阻尼](https://threejs.org/manual/en/rendering-on-demand.html)
- [资源释放与共享资源生命周期](https://threejs.org/manual/en/how-to-dispose-of-objects.html)
- [WebGPURenderer 与迁移边界](https://threejs.org/manual/en/webgpurenderer)
